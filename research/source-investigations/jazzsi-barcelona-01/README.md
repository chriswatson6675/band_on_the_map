# jazzsi-barcelona-01

Explanatory only — `investigation.json` in this directory is authoritative.

Part of `BAND-ON-THE-MAP-BARCELONA-30-VENUE-POPULATION-02`. Reuses
`ingestion/rss/parse.mjs` (link discovery only) and `ingestion/json-ld/`
(event facts) completely unchanged — the same "crawl a listing/feed for
links, then read each page's own JSON-LD" pattern already established for
Sala Apolo (`BARCELONA-30-VENUE-POPULATION-01`).

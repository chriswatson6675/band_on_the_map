# Erith Playhouse — Level 1 passive-static investigation (tranche 2)

Supersedes `triage-osm-node-401788093-london-01`. A single unauthenticated GET to https://www.playhouse.org.uk returned HTTP 403 Forbidden with a generic hosting-level error page — no venue content was ever served. Decision: `DEFER` (BLOCKED at Level 1; policy prohibits retrying/bypassing). See `investigation.json` for the authoritative record.

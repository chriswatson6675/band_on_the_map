# Arizona Lounge — Level 1 passive-static investigation (tranche 2)

Supersedes `triage-osm-node-1614146298-london-01`. A single unauthenticated GET to https://www.arizonalounge.com/ returned only Wix client-side bootstrap HTML; `<head>` metadata confirms a live-music/nightclub identity, but no event data or acquisition path was exposed statically. Decision: `DEFER`. See `investigation.json` for the authoritative record.
